# Batch 39 report

## Counts

Nodes worked: 40. Nodes attached: 4. Nodes uncovered: 36. Total (article, node) attachments: 5 (vasicek-single-factor-model carries two).

## Attached (4 of 40)

- **weight-of-evidence** -> `methods/weight-of-evidence-encoding`. Exact match. The article's opening formula, "`WoEi = ln(DistGood_i / DistBad_i)`", is the node's own definition ("replaces each level of a categorical covariate by the log of its odds relative to the portfolio's overall odds"), and the article's intercept-variability and shrinkage discussion covers the node's unbounded-weight-under-zero-events point.

- **vasicek-single-factor-model** -> `methods/single-factor-credit-risk-model-vasicek-and-belkin` and `methods/vasicek-loan-portfolio-value`. Rejected as an overlapping third candidate: `concepts/vasicek-distribution`, whose own subject is parameter estimation for p and rho rather than the PIT-TTC bridge, and whose quantile function duplicates ground the second attached article covers as its main argument. The two attached articles split the node's two sentences. `single-factor-credit-risk-model-vasicek-and-belkin` carries the one-factor decomposition ("the latent creditworthiness of a borrower... equals the square root of rho times Z, plus the square root of one minus rho times an idiosyncratic draw") and the cycle-conditioned through-the-cycle transition matrix that Belkin's method derives, which is the node's PIT-TTC bridge. `vasicek-loan-portfolio-value` carries the second sentence directly: "the loss quantile at confidence level q is... This formula is a function of only two borrower-level parameters (PD and rho)", used to derive the Basel IRB capital charge, which is "stressing the factor to an extreme quantile turns the same expression into a portfolio capital formula." Neither article states in so many words that averaging the model over the factor recovers the TTC probability exactly; that half of the node's first sentence is left as a partial-scope limit rather than bridged with my own algebra.

- **vintage-analysis** -> `methods/breeden-2016-lifecycle-environment-loan-level-forecasts`. Partial. The article's APC decomposition, "the loan's age on book, its origination vintage, and the prevailing macroeconomic environment", covers the node's three axes directly, and its topics list carries `vintage-analysis` explicitly. Uncovered half: the article does not use the life-table cohort/attained-age/calendar-year mapping the node draws, and its own subject is the more advanced GLM-scorecard embedding of the APC terms rather than vintage analysis as a standalone descriptive technique.

- **variance-function** -> `methods/non-life-ratemaking`. Thin partial, flagged as such. The clause "a GLM generalises the ordinary linear model by replacing the normal error and constant variance with any exponential-family distribution and its associated variance function" states the concept, that each EDF member carries an associated variance function, and its role in the GLM link. Uncovered half: the article gives no table of the mean, variance, variance function and scale parameter for the binomial, Poisson, exponential, gamma and normal cases the node itself enumerates.

## Uncovered (36 of 40)

**No vault content at all (confirmed by index and full-text grep both ways):** uniform-minimum-variance-unbiased-estimator, universal-approximation-theorem (checked against `concepts/deep-learning-foundations` and every ML-topic index hit; none names the theorem or its "approximate any continuous function on a compact set" guarantee), universal-bank, utility-function, unit-linked-contract, unit-pricing, vector-algebra-lines-and-planes, vector-functions-and-quadratic-curves, vasicek-interest-rate-model (the vault's many Vasicek hits are all the credit-risk single-factor model; no Ornstein-Uhlenbeck short-rate term-structure article exists), volatility-estimation-from-market-data, volatility-smile-and-surface, wave-equation-and-laplaces-equation, wholesale-funded-bank, white-noise-process, world-trade-organization, zeroisation, unexpected-loss-capital-requirement-scope (the black-letter UL scope at d424 paragraph 51 is confirmed present in the source markdown but no wiki article states it), unhedged-foreign-currency-retail-exposure-risk-weight and unsettled-and-failed-transaction-risk-weight (both confirmed present in d424, paragraphs 76-77 and 87-88 respectively, neither has a wiki article), write-off (IFRS 9 5.4.4 confirmed absent from both `regulation/ifrs9-financial-instruments` and `concepts/ifrs9-expected-credit-loss`, which cover classification, the three-stage model and SICR but never the write-off/derecognition event itself).

**Passing mentions only, strongest rejected candidate named:** value-at-risk (`regulation/frtb-minimum-capital-market-risk` treats VaR only as the regime FRTB superseded with expected shortfall, never defining VaR's own mechanics; `methods/asrf-capital-foundation` and `methods/creditmetrics-portfolio-model` both use "value-at-risk" in a credit-portfolio sense in service of a different argument, ASRF portfolio invariance and CreditMetrics economic capital respectively, rather than teaching the general market-risk VaR the node's five actuarial-syllabus anchors point to); value-at-risk-weaknesses (same FRTB article gestures at ES replacing VaR but never states VaR's general weaknesses such as non-subadditivity; a full-wiki grep for "subadditiv" and "coherent risk measure" returns nothing, and the registered source `acerbi-tasche-coherence-expected-shortfall-2002` is cited by no article, confirming no coherence-of-risk-measures treatment exists to attach); yield-curves (`concepts/eve-and-nii` and `regulation/eba-gl-2022-14-irrbb-csrbb` both use "yield curve" only as an IRRBB shock input, "re-prices that present value under a shocked yield curve", never explaining the curve's own shape or level); venture-capital (`concepts/bbb-small-business-finance-markets` mentions "venture capital funding" and "VC exit activity" in one clause on equity-market access gaps, never the risk/return/illiquidity characteristics the node needs); weibull-distribution (`methods/lgd-survival-analysis` and `methods/stochastic-claims-reserving` each namedrop Weibull once as a hazard-function alternative that performed worse than the article's chosen model, never teaching the distribution itself); vector-space-rn (`methods/mathematics-for-machine-learning` lists "vector spaces" once in a chapter-contents sentence, never developing the n-tuple space itself); wholesale-market-funding, wholesale-funded-bank and wholesale-funds-transfer-pricing-regime (NSFR and LCR articles use "wholesale funding" only as a liquidity-stability input category; `regulation/bcbs-bank-capital-balance-sheet-distress` mentions funds transfer pricing in one clause on a bank's capital-management toolkit, not as its own subject); why-banks-hold-capital (the same BCBS WP46 article's own subject is empirical capital-adjustment behaviour during distress, not the basic rationale for holding capital); underwriting-purposes and unemployment-causes-and-costs (every vault "underwriting" hit is mortgage, credit or cyber underwriting rather than life/health insurance underwriting; every "unemployment" hit uses it as a stress-testing macro variable, never its causes or costs); warrant (a tightened word-boundary grep excluding "warrant[s] that/a/further" etc. returns zero financial-instrument hits).

**Considered but deliberately not attached despite surface similarity:** with-profits-contract and with-profits-surplus-distribution (`regulation/uk-insurance-supervision`'s with-profits content is the PRA's SS1/14 mutual-waiver governance process, a different subject from bonus/surplus distribution mechanics; `methods/alm-immunisation` carries `with-profits` only as an index topic tag with no body text on it at all, confirming the brief's warning that the index misleads roughly a third of the time).

## Ledger documents proposed (19 entries, 16 reused + 3 new)

Three new: `bcbs-d424-sa-fx-currency-mismatch-retail-multiplier` and `bcbs-d424-sa-unsettled-failed-transactions`, both verified by direct read of `vault/markdown/bcbs/d424.md` against the node's own anchor paragraph before minting; `iasb-ifrs9-standard-write-off-derecognition` (IFRS 9 paragraph 5.4.4), minted only after checking every existing `iasb-ifrs9-*` id and finding the nearest neighbour, `iasb-ifrs9-standard-modification-of-financial-assets`, covers paragraph 5.4.3 (modification) rather than 5.4.4 (write-off).

Reused verbatim, extending `needed_by` with this batch's ids and adding a note where none existed: `bcbs-d424-irb-risk-weight-functions` (unexpected-loss-capital-requirement-scope); `ifoa-sp1-core-reading-2026` (underwriting-purposes, value-at-risk-capital-assessment); `ifoa-cb2-core-reading-2026` (unemployment-causes-and-costs, world-trade-organization); `up-wst221-course-notes` (uniform-minimum-variance-unbiased-estimator); `ifoa-cm1-core-reading-2026` (unit-linked-contract, variable-force-of-interest, with-profits-contract, zeroisation); `ifoa-sp2-core-reading-2026` (unit-pricing, with-profits-surplus-distribution); `dl-actuarial-2026-l04-05-fnn` (universal-approximation-theorem, same lecture as the existing activation-functions claim, different part); `assa-f107-study-material-2026` (universal-bank, value-at-risk, wholesale-funded-bank, wholesale-market-funding, why-banks-hold-capital, yield-curves); `ifoa-cm2-core-reading-2026` (utility-function, vasicek-interest-rate-model); `up-wtw124-course-notes` (vector-algebra-lines-and-planes, vector-functions-and-quadratic-curves, vector-space-rn); `ifoa-sp5-core-reading-2026` (venture-capital); `ifoa-sp6-core-reading-2026` (value-at-risk-weaknesses, volatility-estimation-from-market-data, volatility-smile-and-surface, warrant); `up-wtw386-course-notes` (wave-equation-and-laplaces-equation); `up-wst211-course-notes` (weibull-distribution); `ifoa-cs2-core-reading-2026` (white-noise-process); `assa-f207-study-material-2026` (wholesale-funds-transfer-pricing-regime).

Departures from the anchor-body default: none beyond the standard first-anchor-only rule for multi-anchor nodes (value-at-risk, five anchors, ledgered against `assa.f107` alone; underwriting-purposes, unit-linked-contract, value-at-risk-capital-assessment, vasicek-interest-rate-model, with-profits-contract, with-profits-surplus-distribution and yield-curves each ledgered against their first-listed anchor, remaining anchors named in-note).

## check.py

`1560 nodes, 13 paths, 0 failures`

## Judgement calls the brief did not cover

The `bcbs-d424-irb-risk-weight-functions` entry's document text says "Rules for corporate and bank exposures" (paragraph 51's own section heading), while the node's body says "corporate, sovereign and bank exposure classes together"; sovereign exposures share the same formula only via a footnote rather than the section title. Noted in the ledger entry rather than corrected, following the batch 13 precedent for an anchor that is close but not a literal match. Separately, `variance-function` and `vintage-analysis` were attached as partial coverage on a thinner reading of the cover bar than most of this batch's other decisions: both articles state and use the node's concept in a real sentence rather than a bare namedrop, but neither article's own subject is the node's subject. Flagged in case the gate wants a stricter line on how much daylight is allowed between "the node's subject" and "the article's subject" before partial coverage tips into passing-mention rejection.
