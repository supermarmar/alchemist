# Phase 2 batch 21 attachment report

40 nodes worked. 11 attached, 29 uncovered. 12 total attachments.

## Attached

### irb-risk-quantification-general-standards
Candidates: `regulation/eba-pd-lgd-estimation-framework` (open, matches PD and LGD, silent on EAD), `regulation/eba-pd-lgd-estimation-guidelines` (open, parent overview of the same framework, less specific).
Attached: `regulation/eba-pd-lgd-estimation-framework`. Its margin-of-conservatism section states the node's core claim for PD and LGD: estimates must be "grounded in historical experience," the margin of conservatism corrects for a data period that "may not be fully representative," and the total margin "must be sufficient to ensure that the institution's risk parameters are prudent." Partial-scope note: the article covers PD and LGD only; the EAD general standard is not treated here and is ledgered as part of `bcbs-d424-irb-minimum-requirements`, alongside the black-letter paragraph 214 text this vault article paraphrases through the EU implementing rules.

### irb-risk-weight-function
Candidates: `methods/vasicek-loan-portfolio-value` (open, matches directly), `methods/asrf-capital-foundation` (open, the deeper theoretical justification but one layer removed from the formula itself), `methods/single-factor-credit-risk-model-vasicek-and-belkin` (rejected, extends the model to transition matrices rather than stating the capital formula).
Attached: `methods/vasicek-loan-portfolio-value`. States the loss-quantile formula built on the standard normal CDF and asset correlation, then names the IRB derivation directly: "The Basel IRB risk-weight function is derived directly from the Vasicek formula, with rho estimated as a function of PD... The formula gives the 99.9% conditional loss quantile, which after scaling for LGD and a maturity adjustment yields the capital requirement." A seeded ledger entry, `bcbs-d424-irb-risk-weight-functions`, already lists this node in `needed_by`; see the ledger section below for why that stands unchanged.

### irb-roll-out-requirements
Candidates: `regulation/pra-ss4-24-irb-approach-2026` (open, matches), `regulation/irb-approach` (open, names roll-out only inside a section list, no treatment).
Attached: `regulation/pra-ss4-24-irb-approach-2026`. Its section 3 treatment states the requirement completely: partial use "is permitted where a firm has a clear and credible roll-out plan for extending IRB coverage across material portfolios," and "permanent partial use requires specific PRA approval and is available only in narrow circumstances." That is a full match to the node's "supervisor-approved implementation plan... rather than moving individual portfolios onto internal ratings piecemeal." Worth contrasting with the rejections below: section 3 states this requirement in full, whereas section 7's one-sentence treatment of rating-system documentation (rejected for `irb-rating-system-documentation-requirements`) only establishes that a documentation obligation exists, without saying what it must contain.

### irb-statistical-model-requirements
Candidates: `regulation/eba-machine-learning-irb` (open, partial match).
Attached: `regulation/eba-machine-learning-irb`, as partial coverage. The EBA discussion paper is framed around machine learning specifically, but it grounds its position in the same CRR Article 179 requirement the node states: "Article 179 requires plausible and intuitive estimates," and the EBA's recommendations name "controlled human-judgement overrides" directly, matching the node's "retain meaningful human judgement over its output." Uncovered half: the node's "predictive power and discriminatory ability" and "no material bias" language, which BCBS d424 paragraph 185 states directly, is not stated in comparable terms here; that half is ledgered under `bcbs-d424-irb-minimum-requirements`.

### irb-validation-requirements
Candidates: `regulation/irb-model-validation` (open, matches the requirement and its governance), `methods/irb-model-validation-and-rating-system-quality` (open, matches the LGD and EAD backtesting the first article treats only briefly), `regulation/ecb-irb-validation-reporting` (rejected, a reporting-template specification rather than a statement of the validation requirement itself).
Attached both `regulation/irb-model-validation` and `methods/irb-model-validation-and-rating-system-quality`, as different parts of one node. The first states the requirement and its governance: validation must be "periodic, independent... structured around three core dimensions," feeding into the margin of conservatism when a deviation is found, which answers the node's "deviation between the two requires the estimates to be revised." The second carries the realised-versus-estimated comparison the first treats only for PD: dedicated LGD validation ("comparing the model's mean LGD estimate against the observed average recovery") and EAD validation ("comparing predicted credit conversion factors against out-of-sample observations"), which the node's "loss rates and exposures" language needs and the first article does not supply.

### irb-versus-ifrs9-model-differences
Candidates: `methods/credit-loss-modelling-frameworks` (open, matches directly).
Attached: `methods/credit-loss-modelling-frameworks`. Its "Model reuse and calibration adjustments" section states the node's exact claim: a single scorecard "produces a raw PD that is neither TTC nor PIT; the firm applies an adjustment to produce a TTC PD for A-IRB and a separate PIT adjustment for CECL and IFRS 9," and "the same model must satisfy two sets of validation requirements simultaneously." That is the horizon-conservatism-purpose divergence the node names, built from the same PD, LGD and EAD blocks.

### isotonic-regression
Candidates: `concepts/balance-property-and-auto-calibration` (open, matches directly).
Attached: `concepts/balance-property-and-auto-calibration`. States the node's claim in one sentence: isotonic regression "carries no hyper-parameters, imposes monotonicity, and is empirically auto-calibrated by construction, with over-fitting at the two ends of the range the residual concern." Its own anchor source is the same UCSC lecture (l07) this node is anchored to.

### land-acquisition-development-construction-exposure-risk-weights
Candidates: `regulation/eba-adc-exposures` (open, matches directly).
Attached: `regulation/eba-adc-exposures`. Names the exact mechanism: ADC exposures "attract a default risk weight of 150%... may reduce the risk weight to 100% when specific credit risk-mitigating conditions are met," with the preferential treatment available on a pre-sale or pre-lease route. The node's anchor is the BCBS standardised-approach text itself rather than this EU implementing guideline, but the guideline treats the identical mechanism (the EU-mandated conditions for the same preferential weight CRR3 leaves undefined) in more operational depth than the black-letter paragraph itself would.

### lasso-regularisation
Candidates: `methods/icenet-smoothness-and-monotonicity-constraints` (open, matches directly), `methods/npl-recovery-rate-modelling` (rejected, names Lasso as one compared regression family in a single clause).
Attached: `methods/icenet-smoothness-and-monotonicity-constraints`. States the node's claim exactly: "LASSO adds an L1 penalty whose non-differentiability at the origin sets coefficients exactly to zero, so raising the penalty weight yields a sparse model and an implicit variable ranking." Both node and article trace to the same source lecture (ucsc.dl-actuarial-2026.l08).

### lcr-outflow-assumptions
Candidates: `regulation/bcbs-liquidity-coverage-ratio-original` (open, partial match).
Attached: `regulation/bcbs-liquidity-coverage-ratio-original`, as partial coverage. It states the general shape of the run-off assumptions: the stress scenario combines "an idiosyncratic shock, representing a partial loss of unsecured wholesale funding... and a market-wide shock, representing partial unavailability of secured funding and elevated retail and wholesale deposit outflows," calibrated through "run-off rates and inflow rates." Uncovered half: the specific numeric run-off percentages by liability type, which the node's anchor (ASSA F107) presumably tabulates and this article does not; ledgered below.

### lee-carter-model
Candidates: `methods/mortality-modelling` (open, matches directly).
Attached: `methods/mortality-modelling`. States the model precisely: it "writes the log of age-specific mortality as an age-specific average plus an age-specific sensitivity term multiplying a single time-varying mortality index, fitted originally by singular value decomposition and projected forward as a stochastic time series."

## Uncovered (29)

Grouped by why, with the strongest rejected candidate named where one existed.

**No vault content at all** (confirmed by both index search and full-text grep across `vault/wiki`): issue-specific-versus-issuer-ratings, iterative-methods-for-linear-systems-and-eigenvalue-problems, iterative-methods-for-nonlinear-equations, ito-lemma, joint-life-annuity-and-assurance, joint-life-functions, keynesian-multiplier, labour-market-and-wage-determination, laplace-transform, large-portfolio-reallocation-challenges, laurent-series, law-of-total-expectation, layer-normalisation (the source lecture is ingested, but no wiki article treats this slide section; see ledger note).

**Passing mentions only, rejected:**
- irb-rating-assignment-integrity-requirements: `regulation/pra-ss4-24-irb-approach-2026` section 7 notes rating systems "must be documented, reviewed at least annually, and subject to independent validation" as one clause inside a broader design-standards paragraph; no treatment of independence from the lending decision or recorded overrides.
- irb-rating-grade-structure-requirements: the same article's section 7 covers "the number of rating grades" only as an item in a list of what the section addresses, without the seven and four minimum counts the node states.
- irb-rating-system-definition: `concepts/economic-value-of-rating-systems` discusses rating system quality as a driver of portfolio return and capital, not the two-dimension borrower/facility structure the node defines.
- irb-rating-system-documentation-requirements: the same PRA article states the documentation obligation exists ("rating systems must be documented, reviewed at least annually") without describing what the documentation must contain, which is the node's substance.
- irb-stress-testing-requirements: `regulation/eba-stress-testing-guidelines` and `methods/ccar-stress-testing-credit-models` both treat stress testing at a different scope, institution-wide ICAAP/ILAAP stress testing and US CCAR capital planning respectively, neither addressing the Pillar 1 IRB credit-risk stress test against rating grades the node describes.
- iteratively-reweighted-least-squares: `methods/exponential-dispersion-family-and-glm` names IRLS once as an alternative label for Fisher scoring ("by Fisher scoring or iteratively re-weighted least squares"), with none of the working-response mechanics or Newton-Raphson equivalence the node needs.
- joint-distribution: `methods/default-correlation-copula-models` uses "joint distribution" and "marginal distributions" as terms inside a copula construction for correlated default, not a definition of joint, marginal and conditional distributions as such.
- k-means-clustering: `methods/statistical-learning-with-python` and `methods/probabilistic-machine-learning` both name "clustering" as one item in a textbook's table of contents, with no treatment of the k-means algorithm itself.
- kaplan-meier-estimator: `methods/lgd-survival-analysis` uses the Kaplan-Meier curve as an input to a pseudo-Cox LGD model ("Djurovic's simulation study illustrates how the Kaplan-Meier survival curve, fit to a cohort... produces a recovery-rate extrapolation curve") rather than defining the product-limit estimator or Greenwood's formula.
- kolmogorov-equations: the two candidates found by keyword search, `methods/stochastic-claims-reserving` and `methods/gini-factor-decomposition`, both use "Kolmogorov" as half of "Kolmogorov-Smirnov test," an unrelated statistic.
- lagrange-multipliers: `methods/mathematics-for-machine-learning` names "Lagrange multipliers" once inside a chapter-contents summary, with no treatment of the constrained-optimisation method itself.
- least-squares-estimation: `methods/ols-assumption-violations` assumes least squares already established and treats violations of its Gauss-Markov assumptions; `methods/mathematics-for-machine-learning` frames least squares as a geometric projection result inside a linear-algebra survey. Neither derives the slope and intercept estimates the node describes.
- legacy-information-technology-modernisation: `regulation/ecb-risk-data-aggregation-reporting` names "legacy systems" once as a source of RDARR deficiency, not a treatment of the strategic choice between maintaining legacy IT and investing in new technology.

**Considered and deliberately not attached despite surface similarity:**
- lease-financing: `concepts/pcp-structure-and-residual-value-risk` describes a closely analogous mechanism, a UK retail lender absorbing the shortfall between a guaranteed value and disposal proceeds, but it is scoped to consumer motor finance under FCA conduct rules rather than the general bank credit-risk concept of retaining an interest in a leased asset that the node states.
- leases-under-irb: the same PCP article, plus `concepts/used-vehicle-prices-as-an-ecl-driver` and `regulation/motor-finance-voluntary-termination`, all treat UK retail motor finance under consumer credit and conduct regulation. None addresses the IRB regulatory-capital treatment of a lease (splitting the exposure between guaranteed and unguaranteed residual amounts) that the node needs; that treatment is confirmed present at BCBS d424 paragraphs 298 to 299 on direct inspection and ledgered separately.
- lcr-and-liabilities-strategy: `regulation/pra-ilaap`, `regulation/uk-liquidity-framework` and `regulation/bcbs-liquidity-coverage-ratio-original` all treat the LCR and ILAAP as regulatory requirements; none addresses the strategic feedback loop into a bank's deposit and funding strategy that the node states.

## Ledger (`ledger-21.yaml`)

23 entries: 14 reuse an id already listed in `ledger-ids.yaml` (fields copied programmatically from that file rather than retyped, to keep `union_entries` from reporting a false disagreement), and 9 are new. Of the new entries, three cite Basel III: finalising post-crisis reforms (d424) and one cites the UCSC deep-learning lecture 2 source, all four already ingested in the vault; the source is present and cited elsewhere, so the gap is a missing wiki article rather than a missing acquisition. The remaining four new entries are University of Pretoria course units with no existing ledger id, each carrying a `primary_alternative` per the seeded entries' convention.

Two departures from the anchor-body default rule, both direct-inspection findings rather than guesses. First, `irb-risk-weight-function`, `irb-rating-assignment-integrity-requirements` and its d424 siblings are anchored to Basel III: finalising post-crisis reforms rather than an EU or UK implementing text; that follows the default rule rather than departing from it, since the anchor body is itself d424. Second, `irb-risk-quantification-general-standards`, `irb-statistical-model-requirements` and `lcr-outflow-assumptions` each already carry a vault attachment covering part of their ground; their ledger entries carry only the uncovered remainder rather than the whole claim, following the brief's instruction to attach and ledger the rest.

`bcbs-d424-irb-minimum-requirements` bundles seven nodes whose paragraphs (161 to 214) all sit inside the single Basel chapter "H. Minimum requirements for IRB approach," confirmed by reading the extraction directly rather than inferring it from the anchor numbers: paragraph 154 states the chapter's own twelve-part structure, and each node's paragraph number falls inside one of those twelve parts. `leases-under-irb` (paragraph 298) sits in the same chapter's tenth part but is a distinct enough topic, leasing recognition rather than rating system design, to carry its own entry (`bcbs-d424-irb-leasing-requirements`) rather than being folded in, matching the precedent the existing d424 entries set of one entry per subject rather than one per document.

The seeded `bcbs-d424-irb-risk-weight-functions` entry already lists `irb-risk-weight-function` in `needed_by`, and `ifoa-cs2-core-reading-2026` in `sources/wanted.yaml` already lists `kaplan-meier-estimator`; both are correctly unchanged, since a node's ledger entry and its vault attachment are separate questions and Phase 1 had already anticipated both gaps.

`.venv/bin/python scripts/merge_ledger.py --dry-run` reports "would write 94 entries (4 seeded)" with no complaint. It does report nine field disagreements as informational notes, three of which involve this batch's new ids (`up-ekn110-course-notes`, `dl-actuarial-2026-l02-glm`, `up-wtw218-course-notes`): another batch, run independently and in parallel, proposed a new entry under each of those same ids for a different section of the same document. `union_entries` keeps the first-seen claim and merges both batches' node ids into one `needed_by`, so gap closure for this batch's nodes is unaffected; the merged entry's displayed claim, however, will describe only one batch's topic until the gate reconciles the wording. Flagging this for the gate rather than renaming my ids now, since renaming would not undo the collision already committed by the other batch and would instead turn one flagged, semantically-correct shared entry into two separate ones for what is, in each case, a single document to acquire.
