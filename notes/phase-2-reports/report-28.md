# Phase 2 batch 28 attachment report

40 nodes worked. 10 attached, 30 uncovered. 10 total attachments.

## Attached

### order-statistic
Candidates: `methods/eba-pd-backtesting-methodology` (open, dedicated section), `methods/binomial-backtest-pd` (open, same underlying paper, cites the same fact in passing).
Attached: `methods/eba-pd-backtesting-methodology`. Its "## Order statistics for multi-year miscalibration" section states: "the paper introduces a multi-year calibration check based on order statistics. The second-highest observed annual default rate across the eight-year sample is used as a summary statistic, with its expected value and distributional properties derived under the null of correct calibration." Partial-scope note: this is a specific application (the second order statistic in PD backtesting) rather than the general theory of deriving an order statistic's distribution from the parent random variable's distribution, which the node body states generally. `methods/binomial-backtest-pd` merely references the same fact in one sentence and is left off per the dedicated-article rule.

### original-loss-curve
Candidates: `methods/reinsurance-pricing` (open, matches exactly).
Attached: `methods/reinsurance-pricing`. Its "## Increased limits factors and exposure curves" section states: "Miccolis (1977) formalises the increased limits factor I(k), the ratio of expected losses at limit k to expected losses at the basic limit," matching the node's own alias for the concept ("also called an increased limit factor curve") precisely, including the consistency-test mechanism.

### out-of-sample-validation
Candidates: `methods/exponential-dispersion-family-and-glm` (open, matches exactly, same anchor lecture), `methods/entity-embedding-and-network-ensembling` and `methods/credibility-transformer` (open, use the term but do not define it).
Attached: `methods/exponential-dispersion-family-and-glm`. Its "## Fitting and out-of-sample validation" section states: "A more complex model always attains a smaller in-sample loss than a simpler model nested inside it, so an in-sample comparison is optimistic by construction, and the out-of-sample loss on an independent test set is the operative measure for both validation and model selection." This is drawn from the same UCSC lecture 2 the node anchors to (ucsc.dl-actuarial-2026.l02).

### output-floor
Candidates: `entities/basel-accord-evolution` (open, matches, BCBS-sourced), `regulation/crr-credit-risk-provisions` (open, matches, EU CRR3-specific).
Attached: `entities/basel-accord-evolution`. Its "Basel III finalisation" section states: "the output floor: total risk-weighted assets calculated under internal models may not fall below 72.5% of the amount that would result from applying the standardised approaches, phased in over a transition schedule," sourced to BCBS d424 directly, matching the node's second anchor exactly. `regulation/crr-credit-risk-provisions` covers the same figure through the EU's Article 92 lens and is left off as the less dedicated, jurisdiction-specific duplicate.

### outsourcing-risk-management
Candidates: `regulation/pra-ss2-21-outsourcing-third-party-risk` (open, matches, but unclassified), `regulation/pra-outsourcing-third-party-risk` (open, matches, classified public-free).
Attached: `regulation/pra-outsourcing-third-party-risk`. Its opening states: "SS2/21 sets out what a PRA-regulated firm is expected to do before, during and after handing a function to a third party," with a full chapter-by-chapter account of the pre-outsourcing risk assessment and mitigation regime. The March-2026-dated sibling article covering the same statement was the more current candidate but carries no `confidentiality` field, so `attach_articles.py` refused it (check 11); the classified article covers the same statement's substance and is attached instead.

### over-dispersed-poisson-model
Candidates: `methods/stochastic-claims-reserving` (open, matches, dedicated subsection).
Attached: `methods/stochastic-claims-reserving`. Its "Chain-ladder-consistent models" section states: "The over-dispersed Poisson (ODP) model instead assumes incremental claims follow an over-dispersed Poisson distribution; its bootstrap implementation resamples Pearson residuals to build a full predictive distribution, not just two moments." Partial-scope note: the article does not restate in so many words that the ODP-GLM's point estimates coincide with the chain ladder's (the Renshaw-Verrall 1998 result, separately ledgered as `renshaw-verrall-1998-glm-claims-reserving` in an earlier wave), though the section heading groups ODP under "chain-ladder-consistent models."

### p-spline-mortality-model
Candidates: `methods/mortality-modelling` (open, matches, brief).
Attached: `methods/mortality-modelling`. Its "## Reformulation, cohort effects and P-splines" section states: "Currie's 2007 presentation applies penalised B-splines as a two-dimensional regression basis, using both the Lee-Carter and age-period-cohort models as smoothing targets." Partial-scope note: this is one sentence rather than a full exposition of the smoothing-parameter trade-off the node describes, and the article itself flags Currie's presentation as T5 practitioner weight rather than peer-reviewed.

### parameter-risk
Candidates: `methods/stochastic-claims-reserving` (open, matches, worked definition-in-use).
Attached: `methods/stochastic-claims-reserving`. Its "Clark's maximum likelihood curve-fitting" section states: "most of the LDF method's uncertainty is parameter risk, the difficulty of estimating each accident year's ultimate independently, rather than process risk," with a worked numerical illustration (reserve standard deviation falling from 4,885,707 to 3,422,547 once an exposure base is added).

### partial-dependence-plot
Candidates: `methods/icenet-smoothness-and-monotonicity-constraints` (open, matches exactly, same anchor lecture), `concepts/model-interpretability` (open, book-report style, describes what Molnar's textbook covers rather than exposing PDP itself).
Attached: `methods/icenet-smoothness-and-monotonicity-constraints`. It states: "An individual conditional expectation curve is obtained by holding one policy fixed, sweeping one covariate across a set of representative values, and recording the prediction at each. The partial dependence plot is the average of those curves over the portfolio," drawn from the same UCSC lecture 8 the node anchors to. Partial-scope note: the article does not cover the node's second point, that averaging can hide substantial disagreement among the underlying curves. `concepts/model-interpretability` is a source-summary article about an external textbook's coverage, not exposition in the vault's own prose, and is left off per the precedent set for `mathematics-for-machine-learning` in wave 2's report-26.

### pca
Candidates: `methods/ols-predictor-importance` (open, matches, dedicated "## PCA" section), `methods/supervised-macroeconomic-index` (open, matches, same core point from a companion article), `methods/mathematics-for-machine-learning` (open, book-report style, describes textbook coverage rather than deriving PCA).
Attached: `methods/ols-predictor-importance`. Its "## PCA: dimensionality reduction with a directional risk" section states: "PCA maximises explained variance in the predictor space, but it does not constrain the relationship between components and the target variable," matching the node's own point that "the criterion never references a response." `methods/supervised-macroeconomic-index` makes the same underlying point (PCA as unsupervised, variance-only) from a companion article and is left off as covering the same ground. `methods/mathematics-for-machine-learning` is a source-summary article about an external textbook and is rejected on the same grounds as in `partial-dependence-plot` above.

## Uncovered (30)

Grouped by why, with the strongest rejected candidate named where one existed.

**No vault content at all** (confirmed by both index and full-text grep across `vault/wiki`): operational-risk-merger-and-acquisition-treatment, option-greeks, option-price-bound, option-price-determinant, option-pricing-and-dynamic-hedging, opportunity-cost-and-scarcity, ordinary-differential-equations-and-ivps, ornstein-uhlenbeck-process, outcome-window, par-yield-and-yield-to-maturity, passive-and-active-valuation-approaches, payment-frequency-conversion.

**Passing mentions only, rejected:**
- operational-risk-model-granularity: `regulation/operational-risk-capital-approaches`'s history section names "the granularity of the loss unit" as one item in a five-item list describing the now-superseded 2011 AMA supervisory guidelines; no treatment of model granularity, validation or monitoring in the current standardised-approach regime the article otherwise covers.
- option-payoff-profile: `methods/structural-credit-risk-models` states "equity becomes a call option on the firm's assets" as one clause within an exposition of the Merton structural credit model; no treatment of call/put payoff mechanics or the European/American distinction.
- option-underlying-varieties: `regulation/eba-frtb-main-risk-driver-rts` names "plain vanilla options" as one item in an eight-item instrument-universe list for the FRTB simplified method; no treatment of pricing options on different underlyings.
- options-and-guarantees-management-issues: `methods/alm-immunisation` and `_meta`-registered `cfo-forum-mcev-principles-2016` each name "options and guarantees" once, as an ESG-scenario purpose and an MCEV component line item respectively; neither treats the management issues a provider faces once a product carries one.
- options-hedging: `regulation/ccr-internal-models-method` names "a counterparty selling put options on its own shares to the bank" as a specific-wrong-way-risk example; no treatment of options as a hedging instrument.
- orthogonality: `methods/ols-predictor-importance` and `methods/supervised-macroeconomic-index` both use "orthogonal components" descriptively when explaining PCA (attached above, under `pca`); neither treats orthogonality as an inner-product-space concept in its own right.
- out-of-time-validation: `methods/deep-learning-credit-scoring` and `methods/chang-2024-credit-risk-ml-deep-learning` both name out-of-time validation only as something the papers under review fail to report, a methodological limitation rather than a definition; `methods/pd-rating-scale-calibration` cross-references it as "out-of-time experience" a backtesting method validates against, without defining the split.
- overfitting-and-underfitting: `methods/mathematics-for-machine-learning` lists "overfitting" as one item in a book-chapter-coverage sentence; `methods/nguyen-2025-enhancing-credit-risk-ml` and `methods/khandani-2010-consumer-credit-risk-ml` each use the term operationally (guarded against via cross-validation, pruned against via Gini impurity) without defining it.
- overseas-investment-markets: `regulation/bcbs-core-principles-banking-supervision`'s CP21 section defines country risk and transfer risk in a bank-supervisory-policy frame ("supervisors must determine that banks maintain adequate policies"), a different use from the node's investor-level currency and country risk considerations.
- pandemic-response-strategy: `methods/titon-2026-pandemic-risk-r-shiny` is a mortality-modelling paper (frequency-severity excess-mortality forecasting), not a bank-strategy article; `methods/ifrs9-post-model-adjustments` names Covid-19 only as the period post-model adjustments became prominent, a narrower point about one response mechanism rather than bank strategy generally.
- parametric-hypothesis-tests: `methods/statistical-power-analysis` treats hypothesis-test selection specifically for PD point-prediction (exact binomial, z-score, Jeffreys), a different and narrower subject from the general one-sample/two-sample normal, binomial, Poisson and paired-data framework the node describes.
- partial-and-tranched-credit-protection-treatment: `regulation/pra-securitisation-requirements-cp2-26` names "tranched credit protection" only in the context of a resecuritisation-exemption proposal for MGS loans, a different provision from the proportional/tranched capital split and pari passu mechanism the node describes.
- partial-least-squares: `methods/feed-forward-networks-claim-frequency` names PLS once, alongside PCA, as a "linear ancestor" of representation learning, with no treatment of its construction.
- peer-group-capital-benchmarking: `concepts/benchmarking-ecl-workbook` (itself flagged `confidence: uncertain`, a placeholder pending source inspection) and `regulation/irb-model-validation` both treat ECL/IRB parameter benchmarking (PD, LGD dispersion across peers), a different specific subject from the node's capital-ratio peer benchmarking.

**Considered and deliberately not attached despite surface similarity:**
- output-floor-calculation-scope: `regulation/pra-cp16-22-basel31-consultation` states the general principle that "IM firms apply the same revised standardised approaches for the floor calculation as those used by SA-only firms," but this is a superseded UK consultation proposal, not the BCBS d424 text, and does not enumerate the specific six standardised approaches the node lists.
- output-floor-disclosure-requirements: `regulation/eba-output-floor-opinion` was flagged by keyword search as a candidate (its topics list includes "output-floor") but, on direct read, contains no disclosure content at all; it addresses only the Pillar 2/output-floor interaction under CRD6.
- output-floor-transitional-phase-in: `regulation/crr-credit-risk-provisions` and `regulation/pra-cp16-22-basel31-consultation` both describe a transitional phase-in, but a different one: the EU's CRR3 arrangement running through 2032 and the PRA's now-superseded five-year proposal respectively, neither carrying the BCBS d424 50%-to-72.5%-by-2027 schedule with the 25% RWA-increase cap this node anchors to. Attaching either would misrepresent a different jurisdiction's mechanism as the node's own; ledgered instead.
- pareto-distribution: the only vault hit for "Pareto" is `_meta/sources/embrechts-liu-wang-quantile-risk-sharing-2017`, about Pareto-optimal risk sharing, an unrelated concept sharing only the name.

## Ledger (`ledger-28.yaml`)

22 entries: 15 reuse an existing id from `proposed-ids.yaml` (extending `needed_by` with this batch's nodes and adding a note on the further chapter each needs, since none of the reused claims already covered batch 28's ground) and 7 are new documents. `merge_ledger.py --dry-run` reports "would write 115 entries (4 seeded)" with no complaints; the disagreement notes it printed (`up-wtw114-course-notes`, `up-wst111-course-notes`, `up-wtw354-course-notes`, `up-ekn110-course-notes`, `ifoa-sp1-core-reading-2026` primary_alternative, `dl-actuarial-2026-l02-glm`, `up-wtw218-course-notes`) are pre-existing conflicts between other batches' fragments; my `ifoa-sp1-core-reading-2026` entry copies the seeded claim verbatim and does not touch `primary_alternative`, so it is not the source of that disagreement.

New documents, by anchor body: five BCBS d424 paragraphs not covered by earlier waves' d424 entries (operational risk M&A treatment, para 31; output floor calculation scope, para 6; disclosure, para 8; transitional phase-in, para 9; partial and tranched credit protection, para 200), and two further UCSC deep-learning-summer-school entries (a new dedicated out-of-time-validation topic within the already-partly-ledgered Lecture 2, and a first entry for Lecture 1).

One judgement call the brief did not cover directly: `outcome-window` anchors to `ucsc.dl-actuarial-2026.l01`, a lecture with no existing ledger entry or derived wiki article anywhere in waves 1 or 2, unlike every other lecture in the series (l02 through l12). Wave 1's ledger separately found l01 unreliable as a specific-topic anchor for its age-period-cohort node, substituting an external journal paper instead. Lacking access to the raw slide deck to confirm the content directly, I ledgered against l01 as the brief's default anchor-body rule requires, with `status: in-raw` rather than `ingested` (since no wave has yet derived a wiki article from it) and a note flagging the same reliability concern rather than silently substituting an unverified alternative source.
